# Scenario Layout

Scenario definitions and reproducible inputs should be organised by maturity:

- `synthetic/` for tiny hand-built validation networks
- `small_network/` for early UXsim integration tests
- `town_centre/` for the final OSM-backed case study

